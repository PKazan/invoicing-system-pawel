spring:
  profiles:
    active: dev
  flyway:
    enabled: false

  main:
    allow-bean-definition-overriding: true

logging:
  level:
    pl:
      futurecollars:
        invoicing: INFO
        invoicing.db: DEBUG